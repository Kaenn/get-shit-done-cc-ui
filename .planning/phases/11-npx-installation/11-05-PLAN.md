---
phase: 11-npx-installation
plan: 05
type: execute
wave: 4
depends_on: ["11-02", "11-04"]
files_modified:
  - scripts/bump-version.sh
  - README.md
autonomous: true

must_haves:
  truths:
    - "Version sync script updates npm/package.json alongside other manifests"
    - "Main README includes npx installation instructions"
    - "All version references stay synchronized across releases"
  artifacts:
    - path: "scripts/bump-version.sh"
      provides: "Version synchronization including npm package"
      contains: "npm/package.json"
    - path: "README.md"
      provides: "Updated documentation with npx install option"
      contains: "npx get-shit-done-cc-ui"
  key_links:
    - from: "scripts/bump-version.sh"
      to: "npm/package.json"
      via: "sed version update"
      pattern: "npm/package.json"
---

<objective>
Update version sync script and documentation for npx installation

Purpose: Ensure version stays synchronized across all package manifests (package.json, npm/package.json, Cargo.toml, tauri.conf.json) and document the new npx installation method for users.

Output: Updated bump-version.sh and README.md with npx instructions
</objective>

<execution_context>
@/Users/glenninizan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glenninizan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-npx-installation/11-CONTEXT.md
@.planning/phases/11-npx-installation/11-02-SUMMARY.md
@.planning/phases/11-npx-installation/11-04-SUMMARY.md
@scripts/bump-version.sh
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update version sync script</name>
  <files>scripts/bump-version.sh</files>
  <action>
Modify scripts/bump-version.sh to include npm/package.json:

Add after existing sed commands for package.json:

```bash
# Update npm/package.json
sed -i.bak "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" npm/package.json && rm npm/package.json.bak
```

Also update the echo output to mention npm/package.json:

```bash
echo "Updated: package.json, npm/package.json, Cargo.toml, tauri.conf.json, Info.plist"
```

The script should now update 5 files:
1. package.json (root)
2. npm/package.json (npm wrapper)
3. src-tauri/Cargo.toml
4. src-tauri/tauri.conf.json
5. src-tauri/Info.plist

Verify npm/package.json exists before updating (skip with warning if missing):
```bash
if [ -f "npm/package.json" ]; then
  sed -i.bak "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" npm/package.json && rm npm/package.json.bak
else
  echo "Warning: npm/package.json not found, skipping"
fi
```
  </action>
  <verify>
`grep "npm/package.json" scripts/bump-version.sh` shows the file is included
Running `./scripts/bump-version.sh 0.2.2` updates npm/package.json (then revert)
  </verify>
  <done>bump-version.sh synchronizes version across all 5 manifest files</done>
</task>

<task type="auto">
  <name>Task 2: Update main README with npx instructions</name>
  <files>README.md</files>
  <action>
Add npx installation section to the main README.md, near the top after any existing installation instructions:

```markdown
## Quick Start

Run instantly with npx (no Rust required):

```bash
npx get-shit-done-cc-ui
```

The first run downloads the binary for your platform (~15MB). Subsequent runs start immediately.

### Alternative Installation

**Homebrew (macOS):**
```bash
brew install get-shit-done-cc-ui
```

**From source (requires Rust):**
```bash
git clone https://github.com/glennin-codes/get-shit-done-cc-ui
cd get-shit-done-cc-ui
cargo build --release
```
```

Notes:
- Put npx first as it's the easiest method
- Mention first-run download per CONTEXT.md decision
- Keep existing installation methods as alternatives
- If Homebrew isn't set up yet, mark it as "Coming soon" or remove

Also update any existing installation section to not conflict.

Add supported platforms note:
```markdown
### Supported Platforms

- Linux x64
- macOS x64 (Intel)
- macOS arm64 (Apple Silicon)
- Windows x64
```
  </action>
  <verify>
`grep "npx get-shit-done-cc-ui" README.md` returns the installation command
README includes supported platforms list
  </verify>
  <done>README.md documents npx installation as primary method with platform support info</done>
</task>

<task type="auto">
  <name>Task 3: Final verification checklist</name>
  <files></files>
  <action>
Run through the complete verification checklist from ROADMAP.md success criteria:

1. Check that `npx get-shit-done-cc-ui` command is documented
2. Verify all 4 P0 platforms are mentioned (linux-x64, darwin-x64, darwin-arm64, win32-x64)
3. Verify version is synchronized:
   - `grep '"version"' package.json`
   - `grep '"version"' npm/package.json`
   - `grep '^version' src-tauri/Cargo.toml`
   All should show same version (0.2.1)
4. Verify release automation exists:
   - .github/workflows/build-web-binaries.yml
   - .github/workflows/publish-npm.yml
   - release.yml includes both

Document any gaps found for the SUMMARY.

Note: Actual end-to-end testing requires:
- NPM_TOKEN configured in GitHub secrets
- Pushing a test tag
- Verifying npm package on registry

These can only be done after merging to main. Document as "manual verification needed" in SUMMARY.
  </action>
  <verify>
All 4 success criteria from ROADMAP documented/verified:
1. npx works on fresh machine with Node.js
2. Binary download <30s (documented, actual test after release)
3. P0 platforms supported
4. Version synchronized
5. Release automated
  </verify>
  <done>All Phase 11 success criteria verified or documented as requiring post-merge testing</done>
</task>

</tasks>

<verification>
- [ ] scripts/bump-version.sh includes npm/package.json
- [ ] README.md has npx installation instructions
- [ ] README.md lists supported platforms
- [ ] Version is synchronized across all manifests (currently 0.2.1)
- [ ] All success criteria from ROADMAP.md addressed
</verification>

<success_criteria>
Phase 11 is complete: version sync script updated, documentation complete, and all success criteria either verified or documented for post-merge testing.
</success_criteria>

<output>
After completion, create `.planning/phases/11-npx-installation/11-05-SUMMARY.md`
</output>
