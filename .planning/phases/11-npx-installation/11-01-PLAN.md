---
phase: 11-npx-installation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-web-binary.sh
  - src-tauri/Cargo.toml
autonomous: true

must_haves:
  truths:
    - "gsd-ui-web binary builds successfully for current platform"
    - "Binary runs standalone without Tauri dependencies"
    - "Release profile produces optimized, stripped binary"
  artifacts:
    - path: "scripts/build-web-binary.sh"
      provides: "Cross-platform build script for gsd-ui-web (local validation only)"
      min_lines: 20
  key_links:
    - from: "scripts/build-web-binary.sh"
      to: "src-tauri/Cargo.toml"
      via: "cargo build --bin gsd-ui-web"
      pattern: "cargo build.*--bin.*gsd-ui-web"
---

<objective>
Validate local build of standalone web binary and create build script

Purpose: Ensure the gsd-ui-web binary (defined in Cargo.toml) builds correctly and runs standalone before setting up npm distribution. This is foundational validation that the existing web_main.rs entry point produces a working binary.

Output: Build script for local validation and verified local build of gsd-ui-web binary

Note: This build script is for LOCAL DEVELOPMENT ONLY. The CI workflow (11-03) uses cargo directly via houseabsolute/actions-rust-cross for cross-compilation. The script here validates the build works locally before CI automation.
</objective>

<execution_context>
@/Users/glenninizan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glenninizan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-npx-installation/11-CONTEXT.md
@.planning/phases/11-npx-installation/11-RESEARCH.md
@src-tauri/Cargo.toml
@src-tauri/src/web_main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create build script for web binary</name>
  <files>scripts/build-web-binary.sh</files>
  <action>
Create a shell script that builds the gsd-ui-web binary for LOCAL VALIDATION:

1. Accept optional target argument (default: current platform)
2. Build with release profile: `cargo build --release --bin gsd-ui-web`
3. Display binary size and location after successful build
4. Support targets: current (default), linux-x64, darwin-x64, darwin-arm64, win32-x64

The Cargo.toml already has a [[bin]] for gsd-ui-web pointing to web_main.rs.
The release profile already has optimizations: opt-level="z", lto=true, strip="symbols".

Script should:
- cd to src-tauri before running cargo
- Use --target flag if target specified
- Print final binary path and size
- Exit with cargo's exit code
- Include header comment: "# Local development build script. CI uses houseabsolute/actions-rust-cross directly."
  </action>
  <verify>
`bash scripts/build-web-binary.sh` completes without error
`ls -la src-tauri/target/release/gsd-ui-web` shows binary exists
  </verify>
  <done>Build script creates optimized gsd-ui-web binary in target/release/</done>
</task>

<task type="auto">
  <name>Task 2: Verify binary runs standalone</name>
  <files></files>
  <action>
Test that the built binary runs without Tauri desktop dependencies:

1. Run the binary with --help flag: `./src-tauri/target/release/gsd-ui-web --help`
2. Verify it shows CLI usage (port, host flags from clap)
3. Start the server briefly to verify it binds: run with timeout, check for "Starting" message
4. Note the binary size for the SUMMARY

The binary should work standalone because web_main.rs uses axum (not Tauri) for the web server.
If it fails with missing library errors, document them for future platform-specific handling.
  </action>
  <verify>
`./src-tauri/target/release/gsd-ui-web --help` shows port and host options
Binary size is in 5-25MB range (reasonable for stripped Rust binary with web server)
  </verify>
  <done>gsd-ui-web binary runs standalone, shows CLI help, and starts web server</done>
</task>

</tasks>

<verification>
- [ ] scripts/build-web-binary.sh exists and is executable
- [ ] Build script has header comment clarifying it's for local development only
- [ ] Build script succeeds for current platform
- [ ] Binary exists at src-tauri/target/release/gsd-ui-web
- [ ] Binary runs standalone with --help
- [ ] Binary size is reasonable (5-25MB with current release profile)
</verification>

<success_criteria>
Local build produces working standalone gsd-ui-web binary that can be distributed without Tauri framework dependencies. Script serves as local validation tool separate from CI cross-compilation.
</success_criteria>

<output>
After completion, create `.planning/phases/11-npx-installation/11-01-SUMMARY.md`
</output>
