---
phase: 11-npx-installation
plan: 04
type: execute
wave: 3
depends_on: ["11-03"]
files_modified:
  - .github/workflows/release.yml
  - .github/workflows/publish-npm.yml
autonomous: true

must_haves:
  truths:
    - "npm publishing workflow publishes get-shit-done-cc-ui package on release"
    - "Publishing triggers after binaries are uploaded to GitHub Releases"
    - "Version tag triggers entire release pipeline"
  artifacts:
    - path: ".github/workflows/publish-npm.yml"
      provides: "npm publish workflow with authentication"
      min_lines: 30
      contains: "npm publish"
  key_links:
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/publish-npm.yml"
      via: "workflow_call after create-release"
      pattern: "publish-npm"
---

<objective>
Create automated npm publishing workflow triggered by releases

Purpose: Automate publishing the npm package after binaries are uploaded to GitHub Releases. This ensures the npm package version matches the release and binaries are available for download during postinstall.

Output: npm publishing workflow and updated release pipeline
</objective>

<execution_context>
@/Users/glenninizan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glenninizan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-npx-installation/11-CONTEXT.md
@.planning/phases/11-npx-installation/11-RESEARCH.md
@.planning/phases/11-npx-installation/11-03-SUMMARY.md
@.github/workflows/release.yml
@npm/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create npm publish workflow</name>
  <files>.github/workflows/publish-npm.yml</files>
  <action>
Create workflow for npm publishing:

```yaml
name: Publish npm

on:
  workflow_call:
  workflow_dispatch:

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install npm dependencies
        working-directory: npm
        run: npm install

      - name: Publish to npm
        working-directory: npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
```

Notes:
- Uses Node 20 (LTS)
- registry-url required for authentication
- NPM_TOKEN secret must be configured in GitHub repository settings
- --access public required for scoped packages (though get-shit-done-cc-ui is unscoped)
- working-directory: npm since package.json is in npm/ subdirectory
  </action>
  <verify>
`cat .github/workflows/publish-npm.yml` shows valid YAML
Workflow uses NPM_TOKEN secret
Workflow runs from npm/ directory
  </verify>
  <done>publish-npm.yml workflow exists with npm authentication</done>
</task>

<task type="auto">
  <name>Task 2: Wire npm publishing into release pipeline</name>
  <files>.github/workflows/release.yml</files>
  <action>
Add npm publishing step to release.yml, AFTER create-release job completes:

1. Add new job after create-release:
   ```yaml
   publish-npm:
     name: Publish npm
     needs: [create-release]
     uses: ./.github/workflows/publish-npm.yml
     secrets: inherit
   ```

2. The sequence is now:
   - build-linux, build-macos, build-web-binaries (parallel)
   - create-release (uploads to GitHub Releases)
   - publish-npm (publishes npm package after binaries available)

This ordering ensures:
- Binaries are on GitHub Releases BEFORE npm publish
- postinstall script can download binaries immediately after npm install
- If any build fails, npm publish doesn't happen

Add a comment explaining the sequence:
```yaml
# Publish npm package AFTER binaries are on GitHub Releases
# This ensures postinstall can download binaries immediately
publish-npm:
```
  </action>
  <verify>
`grep -A3 "publish-npm:" .github/workflows/release.yml` shows job with needs: [create-release]
Job order ensures binaries uploaded before npm publish
  </verify>
  <done>release.yml triggers npm publishing after GitHub Release is created</done>
</task>

<task type="auto">
  <name>Task 3: Document NPM_TOKEN secret requirement</name>
  <files>npm/README.md</files>
  <action>
Add a "Release Process" section to npm/README.md explaining:

1. How to create NPM_TOKEN:
   - Go to npmjs.com > Account > Access Tokens
   - Create "Automation" token (for CI/CD)
   - Copy token value

2. How to configure GitHub secret:
   - Go to GitHub repo > Settings > Secrets and variables > Actions
   - New repository secret: NPM_TOKEN = <token value>

3. How to trigger a release:
   - Update version: `./scripts/bump-version.sh X.Y.Z`
   - Commit: `git commit -am "chore: bump version to vX.Y.Z"`
   - Tag: `git tag vX.Y.Z`
   - Push: `git push && git push --tags`

This documentation is for maintainers, not end users.
  </action>
  <verify>
`grep -c "NPM_TOKEN" npm/README.md` returns at least 1
README includes release process documentation
  </verify>
  <done>npm/README.md documents NPM_TOKEN setup and release process</done>
</task>

</tasks>

<verification>
- [ ] .github/workflows/publish-npm.yml exists
- [ ] Workflow authenticates with NPM_TOKEN secret
- [ ] release.yml includes publish-npm job after create-release
- [ ] Job sequence is: builds -> create-release -> publish-npm
- [ ] npm/README.md documents NPM_TOKEN setup
</verification>

<success_criteria>
Pushing a version tag triggers: build all binaries -> upload to GitHub Releases -> publish npm package. The npm package is published only after binaries are available.
</success_criteria>

<output>
After completion, create `.planning/phases/11-npx-installation/11-04-SUMMARY.md`
</output>
