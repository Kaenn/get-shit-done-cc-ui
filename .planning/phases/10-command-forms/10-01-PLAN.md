---
phase: 10-command-forms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/gsd/command-registry.ts
  - src/components/gsd/GSDCommandPanel.tsx
  - src/components/gsd/GSDCommandCategory.tsx
autonomous: true

must_haves:
  truths:
    - "User sees all 27 GSD commands in the Commands panel"
    - "Commands are organized into 7 categories"
    - "Each category shows its command count"
  artifacts:
    - path: "src/lib/gsd/command-registry.ts"
      provides: "Complete command definitions with 27 commands"
      exports: ["GSD_COMMANDS", "getCommandsByCategory", "CommandCategory"]
    - path: "src/components/gsd/GSDCommandPanel.tsx"
      provides: "Renders all 7 categories"
      contains: "GSDCommandCategory"
  key_links:
    - from: "src/components/gsd/GSDCommandPanel.tsx"
      to: "src/lib/gsd/command-registry.ts"
      via: "getCommandsByCategory import"
      pattern: "getCommandsByCategory"
---

<objective>
Expand command registry to include all 27 GSD commands organized into 7 categories

Purpose: Users need access to the complete GSD workflow via the Commands panel (CMD-01, CMD-02)
Output: Full command registry with all commands and updated panel rendering all categories
</objective>

<execution_context>
@/Users/glenninizan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glenninizan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-command-forms/10-CONTEXT.md
@.planning/phases/10-command-forms/10-RESEARCH.md
@src/lib/gsd/command-registry.ts
@src/components/gsd/GSDCommandPanel.tsx
@src/components/gsd/GSDCommandCategory.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand command-registry.ts with all 27 commands in 7 categories</name>
  <files>src/lib/gsd/command-registry.ts</files>
  <action>
Update the command registry to include all 27 GSD commands organized into 7 categories:

1. Update the category type from `'plan' | 'execute' | 'settings'` to support all 7:
   - `project-setup` — new-project, map-codebase, resume-project
   - `phase-lifecycle` — discuss-phase, research-phase, plan-phase, execute-phase, verify-phase
   - `roadmap-ops` — add-phase, insert-phase, reorder-phases
   - `milestone-ops` — new-milestone, audit-milestone, complete-milestone
   - `quick-work` — quick-task, quick-fix
   - `navigation` — progress, help, check-todos, status
   - `configuration` — settings, set-profile, configure

2. Add missing commands to GSD_COMMANDS array. Each command needs:
   - id: kebab-case identifier
   - fullCommand: `/gsd:command-name` format
   - label: Human readable name
   - description: Brief one-line description
   - category: One of the 7 categories above
   - icon: Appropriate Lucide icon
   - parameters: Array of CommandParameter (empty for no-param commands)
   - flags: NEW FIELD - Array of CommandFlag for boolean flags
   - isActive: Function returning boolean based on project state

3. Add CommandFlag interface:
```typescript
export interface CommandFlag {
  name: string;        // e.g., "skip-research"
  flag: string;        // e.g., "--skip-research"
  label: string;       // e.g., "Skip Research"
  description: string; // Tooltip text
}
```

4. Update GSDCommandDefinition to include `flags: CommandFlag[]`

5. Update getCommandsByCategory() to return object with all 7 category keys

Key commands with parameters/flags:
- plan-phase: phase (number), flags: --skip-research, --gaps
- execute-phase: phase (number), flags: --gaps-only
- discuss-phase: phase (number)
- research-phase: phase (number), topic (string)
- verify-phase: phase (number)
- add-phase: name (string), goal (string)
- insert-phase: after (number), name (string), goal (string)
- new-milestone: version (string), goal (string)
- quick-task: description (string)
- quick-fix: issue (string)
- set-profile: profile (string enum: thorough, balanced, fast)
  </action>
  <verify>
TypeScript compiles without errors: `npm run typecheck`
Count commands: `grep -c "id:" src/lib/gsd/command-registry.ts` returns 27
  </verify>
  <done>
command-registry.ts exports 27 commands in 7 categories with parameters and flags defined
  </done>
</task>

<task type="auto">
  <name>Task 2: Update GSDCommandPanel to render all 7 categories</name>
  <files>src/components/gsd/GSDCommandPanel.tsx, src/components/gsd/GSDCommandCategory.tsx</files>
  <action>
1. Update GSDCommandPanel.tsx:
   - Import all 7 categories from getCommandsByCategory()
   - Render GSDCommandCategory for each of the 7 categories
   - Order categories logically: project-setup, phase-lifecycle, roadmap-ops, milestone-ops, quick-work, navigation, configuration
   - Use descriptive labels: "Project Setup", "Phase Lifecycle", "Roadmap Operations", "Milestone Operations", "Quick Work", "Navigation", "Configuration"

2. Update GSDCommandCategory.tsx:
   - Add icons for new categories to CATEGORY_ICONS:
     - project-setup: FolderPlus
     - phase-lifecycle: GitBranch (from lucide-react)
     - roadmap-ops: Map (from lucide-react)
     - milestone-ops: Flag (from lucide-react)
     - quick-work: Zap
     - navigation: Compass (from lucide-react)
     - configuration: Settings

3. Default all categories to expanded (per CONTEXT.md decision)
   - Update initializeCategories in gsdStore.ts to expand all 7 categories
  </action>
  <verify>
App starts without errors: `npm run dev`
UI inspection: Commands panel shows 7 collapsible sections
  </verify>
  <done>
Commands panel displays all 7 categories with their respective commands, all expanded by default
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes
2. `npm run dev` starts without errors
3. Commands panel shows 7 categories
4. Total command count is 27
5. Categories can be collapsed/expanded
</verification>

<success_criteria>
- [ ] Command registry has 27 commands
- [ ] Commands organized into 7 categories
- [ ] Panel renders all 7 categories with labels
- [ ] All categories expanded by default
- [ ] TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-command-forms/10-01-SUMMARY.md`
</output>
